Installing programs for the Java etaus random number generator
in this repository:

The following dependencies are used by the testing programs in
this repository.

GNU Scientific Library.

sudo apt-get install libgsl2
sudo apt-get install libgsl-dev
sudo apt-get install gsl-ref-html

Dieharder package.

sudo apt-get install dieharder
sudo apt-get install libdieharder-dev
sudo apt-get install libdieharder3

Make shell scripts executable.

chmod 755 *.sh

Next compile the programs.

./inst.sh

To de-install the runs test up and down repository:

./deinst.sh

To see how to run etaus Java tests, read TESTING.

